# Intraday LOB Modeling with Conformal Prediction Under Distribution Shift

**TL;DR:** Predict intraday LOB movement; quantify uncertainty under regime shifts using conformal prediction.

## Claims (to reproduce on a small public slice)
- Coverage @ 90% target: (fill) on FI‑2010 subset.  
- Interval width vs baseline split‑conformal: (fill).

## Proof/Receipts
- Preprint (arXiv/OSF/Zenodo): (link)
- Poster/slides in `paper/`
- Full‑page screenshots with date + URLs in `receipts/screenshots/`
- Hashes in `receipts/checksums.txt`

## How to run (minimal demo)
```bash
conda env create -f code/env.yml
conda activate lobcp
bash run.sh
```
